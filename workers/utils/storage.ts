export const uploadToR2 = async (file: File) => {
	// Placeholder
	console.log("Uploading to R2", file.name);
};

export const saveToKV = async (key: string, _value: unknown) => {
	// Placeholder
	console.log("Saving to KV", key);
};
